export const buttonWidth = window.innerWidth < 700 ? window.innerWidth / 2 :
  window.innerWidth / 4

export const buttonMarginTop = window.innerHeight < 700 ? window.innerHeight / 30 :
  window.innerHeight / 40
